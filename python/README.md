# pybmap

Python implementation of the BMAP library and the `bosectl` CLI. See the
[repository README](https://github.com/aaronsb/bosectl#readme) for supported
devices, install steps, and the protocol notes.
